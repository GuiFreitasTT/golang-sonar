name: ci-sonarcloud
on:
  pull_request:
    branches:
      - main

jobs:
  run-ci:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-go@v2
      with:
        go-version: 1.15
    - run: go test -coverprofile=coverage ./go
    
    - name: SonarCloud Scan
      uses: SonarSource/sonarcloud-github-action@master
      with:
        projectKey: GuiFreitasTT_golang-sonar   
        organization: Gui                        
        token: ${{ secrets.SONAR_TOKEN }}        


